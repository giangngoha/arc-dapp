[profile.default]
src     = "src"
out     = "out"
libs    = ["lib"]
# Arc Testnet uses London EVM (same as your Router/Factory deploy)
evm_version = "london"
optimizer   = true
optimizer_runs = 200

[rpc_endpoints]
arc = "https://rpc.testnet.arc.network"
